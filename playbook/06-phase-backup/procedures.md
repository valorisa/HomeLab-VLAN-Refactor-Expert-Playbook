# Phase 06 — Procédures

1. Générer clé SSH sur TrueNAS.
2. Ajouter la clé sur QNAP (utilisateur restreint).
3. Configurer `rsync-truenas-to-qnap.sh`.
4. Planifier dans cron (02:00).
5. Ajouter appel `qnap-shutdown-after-rsync.sh`.
6. Tester restauration partielle.

