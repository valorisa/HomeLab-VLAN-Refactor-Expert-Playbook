# Phase 05 — Procédures

1. Mettre à jour Proxmox (`apt update && apt full-upgrade`).
2. Vérifier `pvecm status`.
3. Déployer VM Windows Server 2022.
4. Promouvoir en DC.
5. Configurer DHCP (scopes + options).
6. Configurer DNS (zones forward/inverse).
7. Sauvegarder la VM (snapshot + export).
